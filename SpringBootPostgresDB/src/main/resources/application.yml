spring:
  port: 8080
  application:
    name: SpringBootPostgresDB
  datasource:
    url: jdbc:postgresql://localhost:5432/productdb
    username: postgres
    password: 1640
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true